<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <title>Document</title>
  </head>
  <body>
    <div class="container mt-5">
      <h1>Login Page</h1>
      <% if (locals.msg) { %>
      <div class="alert alert-danger mt-5" role="alert"><%= locals.msg %></div>
      <% } %>
      <form method="post" action="/users/login" class="row g-3 mt-4">
        <div class="col-auto mt-4">
          <span>Enter your credentials</span>
        </div>

        <div class="col-auto">
          <input
            type="email"
            class="form-control"
            id="inputPassword2"
            name="email"
          />
        </div>
        <div class="col-auto">
          <input
            type="password"
            class="form-control"
            id="inputPassword2"
            name="password"
          />
        </div>
        <div class="col-auto">
          <button type="submit" class="btn btn-primary mb-3">Generate</button>
        </div>
      </form>
    </div>
  </body>
</html>
